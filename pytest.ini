[pytest]
testpaths = backend/test_files
markers =
    integration: tests that require a running backend server or external services
addopts = -v -p no:warnings --tb=no -rN
filterwarnings =
    ignore::Warning

